mutation updateWorkout($id: ID!, $input: UpdateWorkoutInput!) {
  updateWorkout(id: $id, input: $input) {
    id
    activity {
      id
    }
    intensity
    slot
    start
    end
    utcOffset
    key
    competition
    intraFuelling
    intraFuellingPrompt
    source
    status
  }
}
